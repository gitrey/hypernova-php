hypernova.client.url=http://localhost:8080/batch

# Example timeouts (optional, defaults are set in HypernovaClientProperties)
# hypernova.client.connect-timeout=3000
# hypernova.client.read-timeout=3000
